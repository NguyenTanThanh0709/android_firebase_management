<?xml version="1.0" encoding="utf-8"?>
<!-- dialog_add_edit_certificate.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- dialog_add_edit_certificate.xml -->
    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">



        <EditText
            android:id="@+id/name_subject_add"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Tên môn học"/>

        <ImageView
            android:id="@+id/imageViewSubject"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_alignEnd="@id/imageViewAvatar"
            android:layout_alignBottom="@id/imageViewAvatar"
            android:layout_marginEnd="-8dp"
            android:layout_marginBottom="-8dp"
            android:src="@drawable/subject" />

        <EditText
            android:id="@+id/image_subject_add"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:enabled="false"
            android:hint="Ảnh bìa môn học"/>


    </LinearLayout>

</LinearLayout>
